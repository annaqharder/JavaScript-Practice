function replaceDigit(str) {
    return str.replace(/[0-9]/, "$")
}

// function replaceDigit(str) {
//     return str.replace(/\d/, "$")
// }

console.log(replaceDigit("abcd1234)"))

// string.replace(searchValue, newValue)